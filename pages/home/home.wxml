<!--pages/demo/demo.wxml-->
<view>
  <button type="primary"
          size="mini"
          wx:for="{{navBar}}"
          wx:key="index">
    <navigator url="{{item.path}}">{{ item.name}}</navigator>
 </button>
</view>
